import { StyleSheet } from "react-native";
import { colors } from "../../../services";
import { width } from "react-native-dimension";
export const styles= StyleSheet.create({
    text:{
        color:colors.fire,
        marginHorizontal:width(5.44)
    }
})